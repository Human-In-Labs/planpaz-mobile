export function formatHumidity(value: number) {
    return `${value}%`;
}

export function formatUV(value: number) {
    return value.toString();
}

export function formatWind(value: number) {
    return `${value} km/h`;
}